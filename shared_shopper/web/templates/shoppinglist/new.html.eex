<h2>New shopping list</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_shoppinglist_path(@conn, :create, @user),
                        person: ""%>

<%= link "Back", to: user_shoppinglist_path(@conn, :index, @user) %>
